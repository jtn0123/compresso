sonar.projectKey=jtn0123_compresso
sonar.organization=jtn0123ismysonar

# Source directories
sonar.sources=compresso/
sonar.tests=tests/

# Test file patterns
sonar.test.inclusions=**/test_*.py,**/tests/**

# Exclusions (generated/vendored/build artifacts)
sonar.exclusions=**/node_modules/**,**/dist/**,**/.venv/**,**/venv/**,**/__pycache__/**,**/build/**,compresso/webserver/frontend/public/**

# Exclude frontend source from coverage analysis — JS/Vue coverage is tracked
# by vitest in a separate workflow and cannot be consumed here due to
# cross-workflow artifact limitations. Without this exclusion, any formatting
# change to Vue files triggers a "0% coverage on new code" quality gate failure.
sonar.coverage.exclusions=compresso/webserver/frontend/src/**

# Exclude frontend source from duplication analysis — Vue template patterns
# (repeated q-btn, q-card, q-item structures) are inherently duplicated
# and not meaningful duplication in the DRY sense.
sonar.cpd.exclusions=compresso/webserver/frontend/src/**

# Python coverage (unit test shards from the same workflow)
# NOTE: integration-coverage.xml and lcov.info are produced by other workflows
# and cannot be consumed here due to cross-workflow artifact limitations.
# To include them, consolidate all test jobs into a single workflow.
sonar.python.coverage.reportPaths=coverage-shard-1.xml,coverage-shard-2.xml,coverage-shard-3.xml

# JavaScript/Vue coverage (when available in same workflow)
# sonar.javascript.lcov.reportPaths=lcov.info

# Source encoding
sonar.sourceEncoding=UTF-8
