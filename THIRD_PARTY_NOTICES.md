# Third-party notices

Compresso includes and depends on third-party software whose copyright and license terms remain with their respective authors.

The authoritative dependency inventories are:

- `requirements.lock` and `requirements-dev.lock` for Python;
- `compresso/webserver/frontend/package-lock.json` for the frontend;
- `compresso/webserver/package-lock.json` for web build tooling;
- `.github/release/package-lock.json` for release tooling; and
- the CycloneDX/SPDX software bill of materials attached to each published release and container image.

Vendored code and assets must retain their upstream notices. This file is an index to those machine-readable inventories, not a replacement for any dependency's license text and not a claim that third-party work is covered by Compresso's GPL grant.
